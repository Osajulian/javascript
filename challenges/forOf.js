const colors = ['red', 'green', 'blue']

for (let color of colors)
console.log(color);

//Difference between a for-in loop and a for-of loop.
// use the for-in loop to iterate over the properties of an object.
// use the for-of loop to iterate over the elements or items in an array.