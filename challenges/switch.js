function userRole(role) {
    let message = console.log('Hello ' + role);
    // let userType = role
    switch (role) {
        case 'administrator':
            message;
            break;

        case 'standard':
            message;
            break;

        default:
            message;
    }
}

userRole('ADMINISTRATOR');